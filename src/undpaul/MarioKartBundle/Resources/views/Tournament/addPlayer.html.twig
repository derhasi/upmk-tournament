{% extends 'undpaulMarioKartBundle::layout.html.twig' %}

{% block page_title %}
    {{ tournament.name }}: Add player</em>
{% endblock %}

{% block content %}
  {% if form %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
  {% else %}
    Tournament already has started. So we cannot add anymore players.
  {% endif %}
{% endblock %}