{% extends 'undpaulMarioKartBundle::layout.html.twig' %}

{% block breadcrumbs %}
  {{ parent() }}
  <a href="{{ path('undpaul_mario_kart_tournament_index') }}">
    Tournaments
  </a>
  <a href="{{ path('undpaul_mario_kart_tournament_view', { 'tournament_id': tournament.id}) }}">
    {{ tournament.name }}
  </a>
{% endblock %}

{% block page_title %}
    {{ tournament.name }}: Add round</em>
{% endblock %}

{% block content %}
  {% if form %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
  {% else %}
    Cannot generate new round.
  {% endif %}
{% endblock %}