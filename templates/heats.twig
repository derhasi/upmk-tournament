<div class="heats">
  {% for heat in heats %}
  <div class="heat">
    <h2>Heat {{ heat.getID() }}</h2>
    {% for race in heat.races %}
      {% include "race.twig" %}
    {% endfor %}
  </div>
  {% endfor %}
</div>
